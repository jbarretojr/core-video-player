import 'package:flutter/material.dart';

class CoreIconData {
  const CoreIconData(this.icon, {this.size, this.color = Colors.white});

  final IconData icon;
  final double? size;
  final Color? color;
}
